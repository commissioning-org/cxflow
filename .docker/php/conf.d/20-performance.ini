; Small performance tweaks that help framework-heavy apps.

; Realpath cache helps Composer autoload + framework class loading
realpath_cache_size=4096K
realpath_cache_ttl=600

; Keep PHP from doing expensive stat calls too often in dev
; (Laravel still works fine; adjust if you do heavy symlink switching)
